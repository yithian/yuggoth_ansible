{{ ansible_managed|comment }}
#
# /etc/hosts: static lookup table for host names
#

#<ip-address>	<hostname.domain.org>	<hostname>
127.0.0.1	localhost.localdomain	localhost
::1		localhost.localdomain	localhost
{{ pihole_ip }}	{{ pihole_subdomain }}
{{ void_ip }}	{{ void_subdomain }} {{ mail_subdomain }} {{ nc_subdomain }} {{ web_subdomain }}
{{ storage_host_ip }}	{{ storage_subdomain }} {{ storage_subdomain }}.{{ domain }}
{{ workstation_ip }}	{{ workstation_subdomain }} {{ workstation_subdomain }}.{{ domain }}
{{ celaeno_ip }}    {{ celaeno_subdomain }} {{ celaeno_subdomain }}.{{ domain }}
{{ router_ip }}	{{ router_subdomain }}
{{ blog_ip }}	{{ blog_subdomain }}

# End of file
