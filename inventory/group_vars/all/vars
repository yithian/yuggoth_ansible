---
# common to all roles. mostly connection stuff

# this is me!
user: "{{ vault_user }}"
email: "{{ vault_email }}"
uid: "{{ vault_uid }}"
gid: "{{ vault_gid }}"
shell: "{{ vault_shell }}"

# site domain
domain: "{{ vault_domain }}"

router_ip: "{{ vault_router_ip }}"
router_subdomain: "{{ vault_router_subdomain }}"

storage_subdomain: "{{ vault_storage_subdomain }}"
storage_host: "{{ vault_storage_host }}"
storage_host_ip: "{{ vault_storage_host_ip }}"

# cloudflare token for ddclient and certbot
cloudflare_creds_file: "{{ vault_cloudflare_creds_file }}"
cloudflare_api_token: "{{ vault_cloudflare_api_token }}"
cloudflare_ddns_update_file: "{{ vault_cloudflare_ddns_update_file }}"
