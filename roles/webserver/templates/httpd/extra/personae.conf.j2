{{ ansible_managed | comment }}
# configuration for the personae rails app
<VirtualHost *:{{ web_port }}>
    ServerAdmin {{ user }}@{{ domain }}
    ServerName {{ personae_host }}

    Redirect permanent / https://{{ personae_host }}/

    ErrorLog /var/log/httpd/{{ personae_host }}-error_log
    CustomLog /var/log/httpd/{{ personae_host }}-access_log common
</VirtualHost>

<VirtualHost *:{{ ssl_port }}>
    ServerAdmin {{ user }}@{{ domain }}
    ServerName {{ personae_host }}

    ProxyPass / http://localhost:{{ personae_port }}/
    ProxyPassReverse / http://localhost:{{ personae_port }}/

    SSLEngine on
    SSLCertificateFile "/etc/letsencrypt/live/{{ web_host }}/fullchain.pem"
    SSLCertificateKeyFile "/etc/letsencrypt/live/{{ web_host }}/privkey.pem"

    BrowserMatch "MSIE [2-5]" \
         nokeepalive ssl-unclean-shutdown \
         downgrade-1.0 force-response-1.0
    ErrorLog /var/log/httpd/ssl_{{ personae_host }}-error_log
    CustomLog "/var/log/httpd/ssl_{{ personae_host }}-request_log" \
          "%t %h %{SSL_PROTOCOL}x %{SSL_CIPHER}x \"%r\" %b"
</VirtualHost>
